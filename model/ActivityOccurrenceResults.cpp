/**
 * Knetik Platform API Documentation latest 
 * This is the spec for the Knetik API.  Use this in conjunction with the documentation found at https://knetikcloud.com.
 *
 * OpenAPI spec version: latest 
 * Contact: support@knetik.com
 *
 * NOTE: This class is auto generated by the swagger code generator 2.2.3.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */



#include "ActivityOccurrenceResults.h"

namespace com {
namespace knetikcloud {
namespace client {
namespace model {

ActivityOccurrenceResults::ActivityOccurrenceResults()
{
}

ActivityOccurrenceResults::~ActivityOccurrenceResults()
{
}

void ActivityOccurrenceResults::validate()
{
    // TODO: implement validation
}

web::json::value ActivityOccurrenceResults::toJson() const
{
    web::json::value val = web::json::value::object();

    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Users )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        val[U("users")] = web::json::value::array(jsonArray);
    }

    return val;
}

void ActivityOccurrenceResults::fromJson(web::json::value& val)
{
    {
        m_Users.clear();
        std::vector<web::json::value> jsonArray;
        for( auto& item : val[U("users")].as_array() )
        {
            if(item.is_null())
            {
                m_Users.push_back( std::shared_ptr<UserActivityResults>(nullptr) );
            }
            else
            {
                std::shared_ptr<UserActivityResults> newItem(new UserActivityResults());
                newItem->fromJson(item);
                m_Users.push_back( newItem );
            }
        }
    }
}

void ActivityOccurrenceResults::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix[namePrefix.size() - 1] != U('.'))
    {
        namePrefix += U(".");
    }

    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Users )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        multipart->add(ModelBase::toHttpContent(namePrefix + U("users"), web::json::value::array(jsonArray), U("application/json")));
            }
}

void ActivityOccurrenceResults::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix[namePrefix.size() - 1] != U('.'))
    {
        namePrefix += U(".");
    }

    {
        m_Users.clear();

        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(U("users"))));
        for( auto& item : jsonArray.as_array() )
        {
            if(item.is_null())
            {
                m_Users.push_back( std::shared_ptr<UserActivityResults>(nullptr) );
            }
            else
            {
                std::shared_ptr<UserActivityResults> newItem(new UserActivityResults());
                newItem->fromJson(item);
                m_Users.push_back( newItem );
            }
        }
    }
}

std::vector<std::shared_ptr<UserActivityResults>>& ActivityOccurrenceResults::getUsers()
{
    return m_Users;
}

void ActivityOccurrenceResults::setUsers(std::vector<std::shared_ptr<UserActivityResults>> value)
{
    m_Users = value;
    
}
}
}
}
}

